<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:showIn="navigation_view">
    <group
        android:id="@+id/group1"
        android:checkableBehavior="single">
        <item
            android:id="@+id/subtitle1"
            android:title="Paramètre">
            <menu>
                <item
                    android:id="@+id/giroscope_menu"
                    android:icon="@drawable/sensor_gyro"
                    app:actionLayout="@layout/widget_switch"
                    android:title="Giroscope"/>
            </menu>
        </item>
    </group>
    <group
        android:id="@+id/group2"
        android:checkableBehavior="single">
        <item
            android:id="@+id/subtitle2"
            android:title="Autres">
            <menu>
                <item android:id="@+id/favorite"
                    android:icon="@drawable/favorite"
                    android:title="@string/favorite" />
                <item android:id="@+id/about_menu"
                    android:icon="@drawable/info"
                    android:title="@string/about" />
                <item android:id="@+id/close"
                    android:icon="@drawable/left_arrow"
                    android:title="@string/close" />
            </menu>
        </item>
    </group>

</menu>